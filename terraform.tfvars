admin_cidr = "0.0.0.0/0"  # Allow all traffic from outside
public_key_path = "~/.ssh/id_rsa.pub"  # If dont have, use `ssh-keygen -t rsa -b 4096 -C "your_email@example.com"` to generate a new key
aws_region = "us-east-1"
instance_type = "c7i-flex.large"